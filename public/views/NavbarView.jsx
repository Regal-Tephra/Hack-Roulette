/* global React */

const NavbarView = (props) => {
  const navBartext = 'NavBar ------------------------------------------';
  return (
    <div>
     {navBartext}
      <button>Log Out</button>
    </div>
  );
};


window.NavbarView = NavbarView;
